export interface Venda {
  id: string;
  valor: number;
  // Adicione outros campos relevantes se necessário
}

export interface Pedido {
  id: string;
  status: string;
  // Adicione outros campos relevantes se necessário
}
